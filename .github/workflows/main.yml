on:
  push:
    branches:
    - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout GutHub actions
        uses: actions/checkout@v1
        
      - name: Run a one-line script
        run: mkdir "$HOME/.ssh"
      - name: Run a one-line script
        run: echo "${{ secrets.key }}" > "$HOME/.ssh/key"
      - name: Run a one-line script
        run: chmod 600 "$HOME/.ssh/key"
        
      - name: Deploy progect
        run: 'ssh -p 22 -i $HOME/.ssh/key -o StrictHostKeyChecking=no" --archive --compress --delete . shurik@https://f02b-188-163-24-9.ngrok-free.app:/home/shurik/www3/'
        
    
